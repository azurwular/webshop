import styles from '../styles/Home.module.css';
const Home = async () => {
    // Example fetch data or some content display
    return (
        <div>
           <h1 className={styles.title}>Products</h1>
        </div>
    );
};

export default Home;
